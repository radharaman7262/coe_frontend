import React, { Suspense } from 'react';

import MenuMasterPage from './_components';

const MenuMaster = () => (
    <Suspense>
        <MenuMasterPage />
    </Suspense>
);
export default MenuMaster;
